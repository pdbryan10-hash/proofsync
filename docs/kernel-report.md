# The kernel report — one domain, read against every system we hold

Generated by `node kernel/report.mjs --write`. Every claim below points at a captured
file; any claim whose evidence does not resolve is marked **unproven** and scores nothing.

`●` stated · `◐` inferred · `○` absent

## Scores

| System | Vendor | Stated | Inferred | Absent | Unproven | Coverage |
|---|---|---|---|---|---|---|
| Concerto — Bellrock supplier portal | Bellrock | 20 | 2 | 0 | 0 | **98%** |
| VX Suite | Accruent / Verisae | 16 | 4 | 2 | 0 | **86%** |

Coverage is not a quality score for the software. It is how much of the domain the system
states outright rather than leaving to be inferred — which is exactly the work an operating
layer has to do on top of it.

## Side by side

| | Concerto — Bellrock supplier portal | VX Suite |
|---|---|---|
| **job**<br><span title="What is the unit of work, and what identifies it?">What is the unit of work, and what identifies it?</span> | ● `Order number`, e.g. RDDC0024267/2 — order/revision. Rows are `tr[role="link"]`, opened with Enter; the record replaces the grid at the same URL. | ● `WO #` — the work order number, on every export and every screen. |
| **cross_key**<br><span title="Where can THIS system hold the other system’s reference for the same job?">Where can THIS system hold the other system’s reference for the same job?</span> | ● **`Supplier’s ref`** — a grid column AND a search field. The other system’s job number goes here and the order is found again by searching it. | ● `Contractor Reference No.` on the export, and **Set Provider Reference Number** as an action — the mirror image of Concerto’s `Supplier’s ref`. Each system has a slot for the other’s key. |
| **client**<br><span title="Whose job is it — which client, and is that a field or a tenancy?">Whose job is it — which client, and is that a field or a tenancy?</span> | ● A tenancy, not a field: `pbl_form_dba_portfolioid` — the Client workspace selector — holding 19 clients behind the one login. | ● `Client` and `Parent Organisation` columns — a field, not a tenancy. One login sees several. |
| **site**<br><span title="Where is the work, and is there a stable property identifier?">Where is the work, and is there a stable property identifier?</span> | ● `Location of work` on the order; **UPRN** on the PPM certificate review screen, which is a property identifier rather than a typed name. Sites also carry codes (`ABZ050`). | ● `Site No.`, `Site Name` and `Site Type` — a site number, so joinable rather than typed. |
| **supplier**<br><span title="Who is doing the work, and how is that party reached?">Who is doing the work, and how is that party reached?</span> | ● The login is the supplier: SEE Services Ltd plus three trading entities. Operative(s) names the engineer on the grid. | ● `Contractor` on the job; contact details harvested from the contractor screens into a contact book. |
| **requester**<br><span title="Who asked for it, and who may be asked about it?">Who asked for it, and who may be asked about it?</span> | ● `Originator` with an email and a mobile, on the order itself. | ● `Created By`, `Created By Role` and `Reported By` — which is how an operator knows whether it is asking the person with the problem or the agent who logged it. |
| **intake**<br><span title="How does a job arrive, and does the system record that it told anyone?">How does a job arrive, and does the system record that it told anyone?</span> | ● Concerto **emails the order** to the supplier helpdesk mailbox and records in the feed that it did, against a helpdesk reference. Intake can be driven from the mailbox or the portal and the two reconcile. | ◐ No arrival event. Jobs are discovered by polling the outstanding list or an export; nothing states that anyone was told. |
| **acceptance**<br><span title="Is acceptance a stamped event, or must it be inferred from a status?">Is acceptance a stamped event, or must it be inferred from a status?</span> | ● A stamped, named event — `Accept within SLA` in the feed — plus `Date acknowledged` on the record. | ◐ No acceptance event. `Pend. Accept` is a status and `Date Assigned` a timestamp, so acceptance has to be reconstructed from the pair. |
| **priority**<br><span title="How is urgency expressed — code, band, or a phrase in the client’s own words?">How is urgency expressed — code, band, or a phrase in the client’s own words?</span> | ● `Priority of response` as a phrase in the client’s own words — “3 Working Day (excluding weekends)”, “4 Hour”. Working-day arithmetic, not hours. | ● Banded codes — `P2 - 4 hours`, `P5 - 7 days`, plus quote bands. Hours, not working days. |
| **sla**<br><span title="What targets exist, are they per-job, and does the system show required against actual?">What targets exist, are they per-job, and does the system show required against actual?</span> | ● `Response time required` and `Completion time required`, each shown beside its **actual** on the grid and coloured when missed. | ● Three per-job targets, stated in HOURS on the export — `Accept SLA (Hours)`, `Response SLA (Hours)`, `Fix SLA (Hours)` — with 8760 meaning “no target”. Calendar hours, so no working-day arithmetic. |
| **eta**<br><span title="Is there a promised date, and can it be compared with the target?">Is there a promised date, and can it be compared with the target?</span> | ● `Appointments` on the order, and an `Add Appointment` action. The grid filters on with/without appointment. | ● `ETA` on the export and an `Update ETA` action, so a promise can be compared with the target — 462 of 972 ETAs landed beyond the response target. |
| **states**<br><span title="What is the state vocabulary, and how many jobs sit in each?">What is the state vocabulary, and how many jobs sit in each?</span> | ● Filterable: Attended · In progress · **Parts On Order** · Work complete. Plus dashboard tiles for Awaiting Acceptance, Breaching SLA, Overdue, Requiring Action to Complete, Awaiting Certificate, Passed Back to Supplier. | ● A long vocabulary — Assigned · Pend. Accept · Pend. · In Progress · Service Incomplete · No Contractor · Alternate Contractor · Pend Prov Notif — mapped into the canonical set by the importer. |
| **attendance**<br><span title="Is "someone turned up" distinguishable from "the job is done"?">Is "someone turned up" distinguishable from "the job is done"?</span> | ● `Mark job as attended` is its own action, and Attended is its own status — separable from completion. | ○ No attendance state distinct from progress. An engineer having turned up is not separately expressible. |
| **pause**<br><span title="Can the system say a job is stopped and waiting on something?">Can the system say a job is stopped and waiting on something?</span> | ● **`Parts On Order`** is a first-class action and a filterable status. The supplier states the pause; the client sees it. | ◐ `Service Incomplete` means stopped — parts on order, or the engineer left — but only by convention. It held 113 of the open jobs, and reading it as motion made a de-stocked fridge look like a job under way. |
| **recall**<br><span title="Can it express that work failed and came back?">Can it express that work failed and came back?</span> | ◐ Only as a convention in free text — “**Recall of QPIZ0019423** Eng. left x3 tiles broken at Kitchen followed by the last visit on 7/8”. The repeat visit is stated by whoever typed the description, not linked to the original order. Graded absent until the checker found it. | ◐ Recalls appear in the alarm/tag vocabulary (`Recalled`) rather than as a link between jobs. |
| **evidence**<br><span title="Where do certificates, job sheets and photographs attach?">Where do certificates, job sheets and photographs attach?</span> | ● `Add Note/Document/Photo`, and a **Permits** tab carrying `Request a permit`. PPM certificates have their own review screen. | ○ No attachment action seen on the work order. Notes are text. Photographs are asked for **by email** in the call script — “upload a photo or email one to the helpdesk quoting the work order number”. |
| **completion**<br><span title="What closes a job, and is closing separable from evidencing?">What closes a job, and is closing separable from evidencing?</span> | ● `Work complete` — and it is **separate** from `Add Note/Document/Photo`, so “done but undocumented” is a state the system can express. | ● `Job Complete` is an action on the work order, and completion states carry through to invoicing. |
| **history**<br><span title="Is there an attributed, timed record of what happened?">Is there an attributed, timed record of what happened?</span> | ● The **Notes and Activities** feed: every entry attributed and timed to the second — order added, order emailed, accepted. | ● Notes on the record via `Log Note`, timed and attributed — which is what read-back verification reads. |
| **actions**<br><span title="What is the write path — the named actions that change the record?">What is the write path — the named actions that change the record?</span> | ● Ten named actions under `#dropdownMenuButton`. Note the same id sits on the list, so its presence does not prove you are on a record. | ● A long action list on the work order, revealed as hidden panels rather than screens — Log Note, Update ETA, Tags, escalation, NTE, Job Complete and more. |
| **exception**<br><span title="How does the system express escalation, or whose court the ball is in?">How does the system express escalation, or whose court the ball is in?</span> | ◐ **Cost referral** states `Currently with` — whose court the ball is in — but that is the money workflow. No general escalation field seen on an order. | ● First and second level escalation are **stated** — escalated by, type and comment — which is more than Concerto offers on an order. |
| **money**<br><span title="Is there a spend limit, and a route for quotes and uplifts?">Is there a spend limit, and a route for quotes and uplifts?</span> | ● `Mandate` is the spend limit (NTE) on the grid; `Quote required` and `Cost uplift required` are actions; Quotes has its own screen, 103 awaiting. | ● `NTE` on the export with an `Update NTE Values` action; quote work orders are a separate WO Type (312 of 1,137). |
| **verify**<br><span title="After a write, can the record be re-read to prove it landed?">After a write, can the record be re-read to prove it landed?</span> | ● The feed is the read-back: an action lands as an attributed, timed entry that can be re-read and matched. | ● A note written to the record can be re-read from the record — the basis of the write ledger’s INTENDED → SUBMITTED → CONFIRMED. |

## Where they diverge

- **intake** (blocking) — concerto: *direct*; vx: *inferred*
- **acceptance** (blocking) — concerto: *direct*; vx: *inferred*
- **attendance** (material) — concerto: *direct*; vx: *absent*
- **pause** (material) — concerto: *direct*; vx: *inferred*
- **evidence** (blocking) — concerto: *direct*; vx: *absent*
- **exception** (material) — concerto: *inferred*; vx: *direct*

## Evidence

### Concerto — Bellrock supplier portal

| Concept | Grade | Found in | Sample |
|---|---|---|---|
| job | direct | `tour-05-an-order.json` | placeholder":null,"readOnly":false}],"tables":[{"headers":["Order number"],"rows":1},{"headers":["Order date"] |
| cross_key | direct | `screen-supplier-portal.json` | ","Select rows","Select rows","Select rows","Order number","Supplier's ref","Description","Activity descriptio |
| client | direct | `screen-ppm-certificate-reviews.json` | "title":"PPM Reviews","selects":[{"label":"Workspace","id":"pbl_form_dba_portfolioid","name":"pbl_form_dba_por |
| site | direct | `screen-ppm-certificate-reviews.json` | eadOnly":false}],"tables":[{"headers":["Select rows","PPM","UPRN","Site","Status","Order number","Date require |
| supplier | direct | `tour-01-the-dashboard.json` | ning Videos'.\nDashboard\nSEE Services Ltd\nAlso includes : SOUTH EASTERN SERVICES (MECHANICAL)SOUTH EASTERN S |
| requester | direct | `tour-05-an-order.json` | ":1},{"headers":["Location of work"],"rows":1},{"headers":["Originator"],"rows":1},{"headers":["Telephone"],"r |
| intake | direct | `tour-05-an-order.json` | \nChirag Vadhiya\n21 Aug 2026 15:47:58\nOrder RDDC0024267/2 emailed to helpdesk@see-services.com.\nPosted by C |
| acceptance | direct | `tour-05-an-order.json` | elpdesk E-mail\n21 Aug 2026 15:58:02\nOrder RDDC0024267/2 : Accept within SLA\nPosted by Helpdesk E-mail on 21 |
| priority | direct | `tour-02-the-order-list-12-207-live.json` | ngineer new dishwasher on route\n\t\n\t\n21 Aug 2026\n\t\n3 Working Day (excluding weekends)\n\t\n26 Aug 2026  |
| sla | direct | `tour-05-an-order.json` | ug 2026 15:58\nResponse time required\t\n22 Aug 2026 23:47\nCompletion time required\t\n23 Aug 2026 07:47\nApp |
| eta | direct | `screen-supplier-portal.json` | 4a07dd27d3c","text":"YODEL"}]},{"label":"Appointment","id":"pbl_form_dba_appoint","name":"pbl_form_dba_appoint |
| states | direct | `screen-supplier-portal.json` | ,"text":"Remedial"}]},{"label":"Status of work order","id":"pbl_form_dba_status","name":"pbl_form_dba_status", |
| attendance | direct | `tour-10-the-write-path-ten-actions.json` | path — ten actions","body":"Add Appointment · Cost uplift · Mark job as attended · Parts On Order · Assign ope |
| pause | direct | `tour-02-the-order-list-12-207-live.json` | s"},{"value":"3ef359dc-77e7-4f0c-9f33-5f2dd6ea20ff","text":"Parts On Order"},{"value":"c7c539f3-379a-4945-ae57 |
| recall | inferred | `tour-02-the-order-list-12-207-live.json` | ert Street\nPizza Express\nSEE Services Ltd\n01 Bellrock\n**Recall of QPIZ0019423 ** Eng. left x3 tiles broken |
| evidence | direct | `tour-10-the-write-path-ten-actions.json` | Mark job as attended · Parts On Order · Assign operative · Add Note/Document/Photo · Work complete · Quote req |
| completion | direct | `tour-02-the-order-list-12-207-live.json` | r"},{"value":"c7c539f3-379a-4945-ae57-ac2da541c087","text":"Work complete"}]},{"id":"pbl_form_dba_portfolioid" |
| history | direct | `tour-05-an-order.json` | 31\nAssign operative (with appointment) : Phillip Williams\nPosted by Helpdesk E-mail on 21 Aug 2026 15:58:31\ |
| actions | direct | `tour-05-an-order.json` | nd Applications\nHE\nHelpdesk E-mail\n21 Aug 2026 15:58:31\nAssign operative (with appointment) : Phillip Will |
| exception | inferred | `screen-cost-referral.json` | ions","Select rows","Ref","Task order","Title","Raised by","Currently with","Cost referral date","Date raised" |
| money | direct | `screen-supplier-portal.json` | required/actual","Status","Parent job status","Call type","Mandate","Cost uplift","Operative(s)","Appointment" |
| verify | direct | `tour-05-an-order.json` | elpdesk E-mail\n21 Aug 2026 15:58:02\nOrder RDDC0024267/2 : Accept within SLA\nPosted by Helpdesk E-mail on 21 |

### VX Suite

| Concept | Grade | Found in | Sample |
|---|---|---|---|
| job | direct | `exports/action-list.csv` | WO #,Status,Tagged,Escalation Reason,WO Type,Priority,Created By,Created By Role,Reported By,D |
| cross_key | direct | `exports/AllWorkOrders.csv` | te Assigned,Date Started,Date Completed,Parent Organisation,Contractor Reference No.,Invoice Amount,Invoice Da |
| client | direct | `exports/OutstandingWorkOrders.csv` | ategory,Problem Type,Compliance Category,Operational Status,Parent Organisation,Client,Open Links,Watching,Cre |
| site | direct | `exports/action-list.csv` | sponse SLA,Response SLA (Hours),Fix SLA,Fix SLA (Hours),ETA,Site No.,Site Name,Contractor,Category,Subcategory |
| supplier | direct | `vx-contractors/contact-detail.json` | afety Group Limited":{"name":"Shield Safety Group Limited","email":"p.todd@shieldsafety.co.uk","phone":"020 37 |
| requester | direct | `exports/action-list.csv` | Status,Tagged,Escalation Reason,WO Type,Priority,Created By,Created By Role,Reported By,Date/Time Created,Acce |
| intake | inferred | `exports/OutstandingWorkOrders.csv` | Priority,Site No.,Site Type,Site Name,Fix Due Date/Time,ETA,Date/Time Created,Contractor,Category,Subcategory, |
| acceptance | inferred | `exports/AllWorkOrders.csv` | te Name,Contractor,Priority,Date/Time Created,Date Approved,Date Assigned,Date Started,Date Completed,Parent O |
| priority | direct | `exports/action-list.csv` | WO #,Status,Tagged,Escalation Reason,WO Type,Priority,Created By,Created By Role,Reported By,Date/Time Created |
| sla | direct | `exports/action-list.csv` | ty,Created By,Created By Role,Reported By,Date/Time Created,Accept SLA,Accept SLA (Hours),Response SLA,Respons |
| eta | direct | `vx-map/screen-action-update-eta.json` | {"name":"action — Update ETA","url":"https://eam.verisae.co.uk/DataNett/action/ChangeSessionData/pacman/callce |
| states | direct | `vx-map/screen-action-list.json` | 905725","text":"Pend Prov Notif"},{"value":"554902","text":"Service Incomplete"},{"value":"554900","text":"Alt |
| attendance | absent | absence confirmed — searched 40 file(s) |  |
| pause | inferred | `exports/action-list.csv` | ire Risk Assessment,Fire Risk Assessment - Annual 122139339,Service Incomplete,Y,No Response From Contractor,Q |
| recall | inferred | `exports/AllWorkOrders.csv` | m.com quoting the work order number.: Yes,,Service Complete,Recalled,100189,Retail,Eastham,ADT Fire & Security |
| evidence | absent | absence confirmed — searched 40 file(s) |  |
| completion | direct | `vx-map/screen-action-budget-and-wo-data.json` | ndicate_PPM_Assets","text":"Indicate PPM Assets"},{"value":"Job_Complete","text":"Job Complete"},{"value":"Lab |
| history | direct | `vx-map/screen-action-log-note.json` | {"name":"action — Log Note","url":"https://eam.verisae.co.uk/DataNett/action/ChangeSessionData/pacman/callcent |
| actions | direct | `vx-map/screen-action-list.json` | {"name":"action list","url":"https://eam.verisae.co.uk/DataNett/action/pacman/callcenter/action_list","ti |
| exception | direct | `exports/OutstandingWorkOrders.csv` | ys),Account Name,Account Description,1st Level Escalated By,1st Level Escalation Type,1st Level Escalation Com |
| money | direct | `vx-map/screen-action-update-nte-values.json` | {"name":"action — Update NTE Values","url":"https://eam.verisae.co.uk/DataNett/action/ChangeSessionData/pacman |
| verify | direct | `vx-map/screen-action-log-note.json` | {"name":"action — Log Note","url":"https://eam.verisae.co.uk/DataNett/action/ChangeSessionData/pacman/callcent |

